# 🎧 Глобальные горячие клавиши для управления Spotify (Без API)

Простой Python-скрипт, который позволяет управлять воспроизведением, переключением треков и громкостью в Spotify (или другом медиаплеере, поддерживающем стандартные мультимедийные клавиши) с помощью глобальных горячих клавиш.

Скрипт использует библиотеки `keyboard` и `pynput` для имитации нажатий стандартных медиа-клавиш операционной системы, реагируя на заданные комбинации `Ctrl + Alt + [Клавиша]`.

## ✨ Возможности

  * **Глобальный охват:** Горячие клавиши работают вне зависимости от того, какое окно активно.
  * **Использование медиа-клавиш:** Управление осуществляется через стандартные мультимедийные клавиши ($Key.media\_play\_pause$, $Key.media\_next$, $Key.media\_volume\_up$ и т.д.).
  * **Настраиваемая задержка:** Имеется задержка в 1 секунду перед отправкой команды (параметр `COMMAND_DELAY_SECONDS`), что может быть полезно для стабильной работы.

## 🚀 Установка и запуск

### 1\. Предварительные требования

Для работы скрипта требуются следующие библиотеки:

```bash
pip install keyboard pynput
```

### 2\. Запуск

1.  Сохраните код как `Spotify_hotkeys.py`.
2.  Запустите скрипт через консоль:

<!-- end list -->

```bash
python Spotify_hotkeys.py
```

3.  Скрипт начнет работать в фоновом режиме, ожидая нажатия горячих клавиш.
4.  Для остановки скрипта нажмите `Ctrl + C` в окне консоли.

## 🎹 Горячие клавиши

| Действие | Клавиша | Функция в коде |
| :--- | :--- | :--- |
| **Плей / Пауза** | `Ctrl` + `Alt` + `Z` | `custom_play_pause()` |
| **Следующий трек** | `Ctrl` + `Alt` + `E` | `custom_next_track()` |
| **Предыдущий трек** | `Ctrl` + `Alt` + `Q` | `custom_previous_track()` |

## ⚙️ Настройки

В файле `Spotify_hotkeys.py` вы можете изменить параметр задержки:

```python
# --- НАСТРОЙКИ ---
# Задержка перед выполнением команды
COMMAND_DELAY_SECONDS = 1 
```
